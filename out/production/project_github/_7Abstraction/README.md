-Interface in java is a mechanism to achieve abstraction and multiple inheritance

-the abstract keyword applies only to classes and methods, indicating that they cannot be
instantiated directly and must be implemented. When we decide on a type of entity by its 
behaviour and not via attribute we should define it as an interface.

-class extends class
class implements interface
interface extends interface

-inheritance
no constructors
multiple inheritance
variables are public, static and final only unlike class which can have instance variables
methods are abstract by default
u can't create an object unlike class
